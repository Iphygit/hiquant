# Reserved for local tooling introduced in a later phase.
# The Supabase anonymous key is public by design, but the service-role key
# must never be stored in this project or shipped to the browser.
SUPABASE_URL=
SUPABASE_ANON_KEY=

